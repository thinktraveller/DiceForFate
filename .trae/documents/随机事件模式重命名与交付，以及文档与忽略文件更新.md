## 目标
- 将主界面“抽签模式”更名为“随机事件模式”（UI文本与相关文档说明）。
- 构建并交付新的 APK（v1.6.0 debug）。
- 在项目文档中添加“更新日志”章节，记录近期版本变更。
- 添加 `.gitignore`，忽略构建缓存、临时工具与交付产物等。

## 变更点
- 资源：`strings.xml` 新增 `lottery_mode_label` 值为“随机事件模式”。
- 布局：`activity_main.xml` 顶部 TextView 文案改为引用新字符串，替换硬编码“抽签模式”。
- 文档：`README.md` 末尾追加“更新日志”节，记录 v1.1.0 ~ v1.6.0 的关键变化（包含本次更名）。
- 忽略文件：根目录新增 `.gitignore`，忽略 `.gradle/`、`app/build/`、`android-sdk/`、`tools/`、`dist/*.apk`、`dist/checksums.txt`、`archive/*.zip`、`.idea/`、`*.iml` 等。

## 构建与交付
- 由于之前清理过本地 SDK，临时安装 Android SDK 命令行工具及必要组件（licenses、platform-tools、platforms;android-34、build-tools）。
- 使用 Gradle Wrapper 构建：`./gradlew assembleDebug`。
- 复制并重命名到：`dist/DiceForFate-v1.6.0-debug.apk`，生成并可选更新校验文件。

## 验证
- 运行构建并确保成功；检查主界面显示“随机事件模式”；抽签功能与普通模式回归正常。
- 最终输出 APK 路径与 SHA-256 校验，更新 README 与 .gitignore 均已就绪。